includes:
    - ./vendor/nunomaduro/larastan/extension.neon

parameters:
    paths:
        - app

    level: max

    excludePaths:
    parameters:
        ignoreErrors:
            -
                identifier: missingType.iterableValue, missingType.generics
