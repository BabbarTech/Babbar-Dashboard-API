includes:
    - ./vendor/nunomaduro/larastan/extension.neon

parameters:
    paths:
        - app

    level: max

    excludePaths:

    ignoreErrors:
    	- missingType.iterableValue
    	- missingType.generics
